version: "3.7"
services:
    server:
        restart: unless-stopped
        build: .
        ports:
            - "5000:5000"
        volumes:
            - instance:/usr/src/app/instance

volumes:
    instance:
